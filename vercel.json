{
  "version": 2,
  "builds": [
    {
      "src": "api/*.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/processar_dados.py" },
    { "src": "/(.*)", "dest": "/$1" }
  ]
}
